import React from 'react';

const Filter = () => {
    return (
        <div>
            {/* Do we want to be able to filter the recipes by cuisine/category? */}
        </div>
    );
};

export default Filter;